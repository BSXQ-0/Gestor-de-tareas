spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gestor_tareas?useSSL=false&serverTimezone=UTC
    username: brayan
    password: Mysql2026*
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
